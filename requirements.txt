# Pinned exactly. Reviewers reproduce with:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# torch is pinned to a CUDA 12.1 build; for a different driver install torch
# separately from https://pytorch.org and then `pip install -r requirements.txt
# --no-deps` for the rest. CPU-only torch will run the CNN study but far too
# slowly to be useful.

numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
scikit-learn==1.7.2
PyWavelets==1.8.0
Pillow==12.2.0
matplotlib==3.10.9
hydra-core==1.3.5
omegaconf==2.3.0
tqdm==4.67.1
joblib==1.5.2
pytest==8.4.2

# CNN steganalysis (Reviewer 1, comment 3). GPU strongly recommended.
#
# CUDA 12.1 wheel. For the LOCAL CPU path (env=local, which is the default and
# covers the GA, SRM and ensemble studies) install the CPU wheel instead --
# mixing a CUDA build into a CPU-only run pulls ~2.5 GB of unused libraries and
# changes nothing about the numbers:
#     pip install torch==2.5.1 torchvision==0.20.1 \
#         --index-url https://download.pytorch.org/whl/cpu
# On Apple Silicon use the CPU wheel: MPS is refused for reported runs.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1

# Execution-target monitoring (§13). Optional: memory_pressure() degrades to
# /proc parsing on Linux without it, and returns load average only on macOS.
psutil==6.1.1

# Kaggle acquisition (§14). Only needed for dataset=bossbase_kaggle.
kaggle==1.6.17
kagglehub==0.3.6

# Experiment tracking (skill §6, §11) and hyper-parameter search.
# Enable via tracking=wandb and search.enabled=true.
wandb==0.18.7
optuna==4.1.0
